version: 2
sql:
  - schema: "migrations/"
    queries: "queries/"
    engine: "sqlite"
    gen:
      go:
        package: "dao"
        out: "../dao"
        overrides:
          - column: events.event_data
            go_type:
              type: string

